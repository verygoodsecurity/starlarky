# For compatibility help with Python, introduced globals are going to be using
# this as a namespace

larky = struct(
    struct=struct,
    mutablestruct=mutablestruct,
    callablestruct=callablestruct,
    descriptor=descriptor,
)