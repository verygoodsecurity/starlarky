def IniLexer(RegexLexer):
    """

        Lexer for configuration files in INI style.
    
    """
    def analyse_text(text):
        """
        '\n'
        """
def RegeditLexer(RegexLexer):
    """

        Lexer for `Windows Registry
        <http://en.wikipedia.org/wiki/Windows_Registry#.REG_files>`_ files produced
        by regedit.

        .. versionadded:: 1.6
    
    """
    def analyse_text(text):
        """
        'Windows Registry Editor'
        """
def PropertiesLexer(RegexLexer):
    """

        Lexer for configuration files in Java's properties format.

        Note: trailing whitespace counts as part of the value as per spec

        .. versionadded:: 1.4
    
    """
def _rx_indent(level):
    """
     Kconfig *always* interprets a tab as 8 spaces, so this is the default.
     Edit this if you are in an environment where KconfigLexer gets expanded
     input (tabs expanded to spaces) and the expansion tab width is != 8,
     e.g. in connection with Trac (trac.ini, [mimeviewer], tab_width).
     Value range here is 2 <= {tab_width} <= 8.

    """
def KconfigLexer(RegexLexer):
    """

        For Linux-style Kconfig files.

        .. versionadded:: 1.6
    
    """
    def call_indent(level):
        """
         If indentation >= {level} is detected, enter state 'indent{level}'

        """
    def do_indent(level):
        """
         Print paragraphs of indentation level >= {level} as String.Doc,
         ignoring blank lines. Then return to 'root' state.

        """
def Cfengine3Lexer(RegexLexer):
    """

        Lexer for `CFEngine3 <http://cfengine.org>`_ policy files.

        .. versionadded:: 1.5
    
    """
def ApacheConfLexer(RegexLexer):
    """

        Lexer for configuration files following the Apache config file
        format.

        .. versionadded:: 0.6
    
    """
def SquidConfLexer(RegexLexer):
    """

        Lexer for `squid <http://www.squid-cache.org/>`_ configuration files.

        .. versionadded:: 0.9
    
    """
def NginxConfLexer(RegexLexer):
    """

        Lexer for `Nginx <http://nginx.net/>`_ configuration files.

        .. versionadded:: 0.11
    
    """
def LighttpdConfLexer(RegexLexer):
    """

        Lexer for `Lighttpd <http://lighttpd.net/>`_ configuration files.

        .. versionadded:: 0.11
    
    """
def DockerLexer(RegexLexer):
    """

        Lexer for `Docker <http://docker.io>`_ configuration files.

        .. versionadded:: 2.0
    
    """
def TerraformLexer(RegexLexer):
    """

        Lexer for `terraformi .tf files <https://www.terraform.io/>`_.

        .. versionadded:: 2.1
    
    """
def TermcapLexer(RegexLexer):
    """

        Lexer for termcap database source.

        This is very simple and minimal.

        .. versionadded:: 2.1
    
    """
def TerminfoLexer(RegexLexer):
    """

        Lexer for terminfo database source.

        This is very simple and minimal.

        .. versionadded:: 2.1
    
    """
def PkgConfigLexer(RegexLexer):
    """

        Lexer for `pkg-config
        <http://www.freedesktop.org/wiki/Software/pkg-config/>`_
        (see also `manual page <http://linux.die.net/man/1/pkg-config>`_).

        .. versionadded:: 2.1
    
    """
def PacmanConfLexer(RegexLexer):
    """

        Lexer for `pacman.conf
        <https://www.archlinux.org/pacman/pacman.conf.5.html>`_.

        Actually, IniLexer works almost fine for this format,
        but it yield error token. It is because pacman.conf has
        a form without assignment like:

            UseSyslog
            Color
            TotalDownload
            CheckSpace
            VerbosePkgLists

        These are flags to switch on.

        .. versionadded:: 2.1
    
    """
def AugeasLexer(RegexLexer):
    """

        Lexer for `Augeas <http://augeas.net>`_.

        .. versionadded:: 2.4
    
    """
def TOMLLexer(RegexLexer):
    """

        Lexer for `TOML <https://github.com/toml-lang/toml>`_, a simple language
        for config files.

        .. versionadded:: 2.4
    
    """
def SingularityLexer(RegexLexer):
    """

        Lexer for `Singularity definition files
        <https://www.sylabs.io/guides/3.0/user-guide/definition_files.html>`_.

        .. versionadded:: 2.6
    
    """
