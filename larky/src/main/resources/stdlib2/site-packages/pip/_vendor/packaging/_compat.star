def with_metaclass(meta, *bases):
    """
     type: (Type[Any], Tuple[Type[Any], ...]) -> Any

    """
    def metadef(meta):  # type: ignore
    """
     type: ignore
    """
        def __new__(cls, name, this_bases, d):
            """
             type: (Type[Any], str, Tuple[Any], Dict[Any, Any]) -> Any

            """
