def Node(object):
    """
    if isinstance(value, list):
        if len(value) == 0:
            value = '<empty>'
        elif len(value) == 1:
            value = '<1 item>'
        else:
            value = '<%d items>' % len(value)
    else:
        if len(value) > 75:
            value = repr(value[:70]+u' ... ')
        else:
            value = repr(value)

    """
def ScalarNode(Node):
    """
    'scalar'
    """
2021-03-02 20:52:11,035 : INFO : tokenize_signature : --> do i ever get here?
    def __init__(self, tag, value,
            start_mark=None, end_mark=None, style=None):
        """
        'sequence'
        """
def MappingNode(CollectionNode):
    """
    'mapping'
    """
