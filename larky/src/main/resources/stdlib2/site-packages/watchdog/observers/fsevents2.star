def FSEventsQueue(Thread):
    """
     Low level FSEvents client. 
    """
    def __init__(self, path):
        """
        'utf-8'
        """
    def run(self):
        """
        FSEvents. Could not start stream.
        """
    def stop(self):
        """
        FSEvents callback. Got %d events:
        """
    def read_events(self):
        """

                Returns a list or one or more events, or None if there are no more
                events to be read.
        
        """
def NativeEvent(object):
    """
    Created
    """
    def __repr__(self):
        """
        <%s: path=%s, type=%s, is_dir=%s, flags=%s, id=%s>
        """
def FSEventsEmitter(EventEmitter):
    """

        FSEvents based event emitter. Handles conversion of native events.
    
    """
    def __init__(self, event_queue, watch, timeout=DEFAULT_EMITTER_TIMEOUT):
        """
         For some reason the create and remove flags are sometimes also
         set for rename and modify type events, so let those take
         precedence.

        """
def FSEventsObserver2(BaseObserver):
