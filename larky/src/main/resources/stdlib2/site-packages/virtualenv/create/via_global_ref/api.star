def ViaGlobalRefMeta(CreatorMeta):
    """
    the filesystem does not supports symlink
    """
    def can_copy(self):
        """
         Priority of where the option is set to follow the order: CLI, env var, file, hardcoded.
         If both set at same level prefers copy over symlink.

        """
    def add_parser_arguments(cls, parser, interpreter, meta, app_data):
        """
        --system-site-packages
        """
    def create(self):
        """
        _virtualenv.pth
        """
    def env_patch_text(self):
        """
        Patch the distutils package to not be derailed by its configuration files
        """
    def _args(self):
        """
        global
        """
    def set_pyenv_cfg(self):
        """
        include-system-site-packages
        """
