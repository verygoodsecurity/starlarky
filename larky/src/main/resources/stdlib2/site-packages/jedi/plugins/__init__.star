def _PluginManager(object):
    """

            Makes it possible to register your plugin.
        
    """
    def decorate(self, name=None):
        """
         Need to reverse so the first plugin is run first.

        """
