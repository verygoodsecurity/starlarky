def TokenType(object):
    """
    '%s(%s)'
    """
def TokenTypes(object):
    """

        Basically an enum, but Python 2 doesn't have enums in the standard library.
    
    """
    def __init__(self, names, contains_syntax):
        """
        'STRING'
        """
