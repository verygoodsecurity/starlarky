def Symbols(object):
    """
    Initializer.

            Creates an attribute for each grammar symbol (nonterminal),
            whose value is the symbol's type (an int >= 256).
        
    """
def _python_symbols(Symbols):
    """
    os.PathLike[str]
    """
