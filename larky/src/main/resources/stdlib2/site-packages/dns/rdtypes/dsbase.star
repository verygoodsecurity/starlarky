def DSBase(dns.rdata.Rdata):
    """
    Base class for rdata that is like a DS record

        @ivar key_tag: the key tag
        @type key_tag: int
        @ivar algorithm: the algorithm
        @type algorithm: int
        @ivar digest_type: the digest type
        @type digest_type: int
        @ivar digest: the digest
        @type digest: int
        @see: draft-ietf-dnsext-delegation-signer-14.txt
    """
2021-03-02 20:53:05,021 : INFO : tokenize_signature : --> do i ever get here?
    def __init__(self, rdclass, rdtype, key_tag, algorithm, digest_type,
                 digest):
        """
        '%d %d %d %s'
        """
    def from_text(cls, rdclass, rdtype, tok, origin=None, relativize=True):
        """
        b''
        """
    def to_wire(self, file, compress=None, origin=None):
        """
        !HBB
        """
    def from_wire(cls, rdclass, rdtype, wire, current, rdlen, origin=None):
        """
        !HBB
        """
