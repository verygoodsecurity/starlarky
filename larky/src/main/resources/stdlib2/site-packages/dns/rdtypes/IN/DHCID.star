def DHCID(dns.rdata.Rdata):
    """
    DHCID record

        @ivar data: the data (the content of the RR is opaque as far as the
        DNS is concerned)
        @type data: string
        @see: RFC 4701
    """
    def __init__(self, rdclass, rdtype, data):
        """
        b''
        """
    def to_wire(self, file, compress=None, origin=None):
