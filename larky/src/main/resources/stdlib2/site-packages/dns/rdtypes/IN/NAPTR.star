def _write_string(file, s):
    """
    '!B'
    """
def _sanitize(value):
    """
    NAPTR record

        @ivar order: order
        @type order: int
        @ivar preference: preference
        @type preference: int
        @ivar flags: flags
        @type flags: string
        @ivar service: service
        @type service: string
        @ivar regexp: regular expression
        @type regexp: string
        @ivar replacement: replacement name
        @type replacement: dns.name.Name object
        @see: RFC 3403
    """
2021-03-02 20:53:02,587 : INFO : tokenize_signature : --> do i ever get here?
    def __init__(self, rdclass, rdtype, order, preference, flags, service,
                 regexp, replacement):
        """
        '%d %d "%s" "%s" "%s" %s'
        """
    def from_text(cls, rdclass, rdtype, tok, origin=None, relativize=True):
        """
        !HH
        """
    def from_wire(cls, rdclass, rdtype, wire, current, rdlen, origin=None):
        """
        '!HH'
        """
    def choose_relativity(self, origin=None, relativize=True):
