def AAAA(dns.rdata.Rdata):
    """
    AAAA record.

        @ivar address: an IPv6 address
        @type address: string (in the standard IPv6 format)
    """
    def __init__(self, rdclass, rdtype, address):
        """
         check that it's OK

        """
    def to_text(self, origin=None, relativize=True, **kw):
