def A(dns.rdata.Rdata):
    """
    A record.

        @ivar address: an IPv4 address
        @type address: string (in the standard "dotted quad" format)
    """
    def __init__(self, rdclass, rdtype, address):
        """
         check that it's OK

        """
    def to_text(self, origin=None, relativize=True, **kw):
