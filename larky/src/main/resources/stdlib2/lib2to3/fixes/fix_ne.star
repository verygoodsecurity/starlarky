def FixNe(fixer_base.BaseFix):
    """
     This is so simple that we don't need the pattern compiler.


    """
    def match(self, node):
        """
         Override

        """
    def transform(self, node, results):
        """
        !=
        """
